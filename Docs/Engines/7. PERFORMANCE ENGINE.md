### ⚡ FLOWMAPPER v1 — PERFORMANCE ENGINE

----------

### 🧠 1. VISÃO DO COMPONENTE

O Performance Engine garante que o FlowMapper:

> não recalcula o mesmo Flow duas vezes e escala bem em soluções grandes.

Ele é responsável por:

-   ⚡ Cache de Flow
-   🔁 Reuso de análise entre builds
-   🧠 Redução de custo do Roslyn Semantic Model
-   📦 Deduplicação de geração de código
-   🚀 Incremental performance real

----------

### 🎯 2. OBJETIVO

O engine deve:

-   💾 evitar recomputar MappingCandidate repetido
-   🔁 reutilizar Flow já construído
-   ⚡ reduzir chamadas ao SemanticModel
-   🧠 garantir incremental execution eficiente
-   📉 minimizar alocação de objetos
-   🧾 garantir output determinístico

----------

### 🚫 3. FORA DE ESCOPO (v1)

Não entra aqui:

-   distributed cache
-   persistência em disco
-   cache cross-solution
-   otimização agressiva JIT
-   parallel graph execution avançada

----------

### 🧱 4. NOVO CONCEITO: FLOW CACHE

### 📦 FlowCache

Responsável por armazenar flows já calculados.

----------

### Estrutura:

```
FlowCache
├── Key
├── Flow
└── Metadata
```

----------

### 🧠 Key (IMPORTANTÍSSIMO)

A chave do cache define tudo:

```
SourceType + DestinationType + Policy + Version
```

----------

### ⚙️ 5. FLOW CACHE STRATEGY

### 🔵 1. CACHE HIT

Se já existe:

```
Flow exists → reuse
```

✔ não recalcula propriedades  
✔ não roda flatten novamente  
✔ não chama semantic model

----------

### 🔴 2. CACHE MISS

```
Flow does not exist → build
```

✔ roda pipeline completo  
✔ salva no cache

----------

### 🧠 6. INCREMENTAL OPTIMIZATION (ROSYLN)

Agora usamos o poder do:

> IIncrementalGenerator + Value-based equality

----------

### STEP 1 — INPUT SHAPING

Evitar recalcular semantic model:

```
SyntaxNode → lightweight key → semantic only if needed
```

----------

### STEP 2 — VALUE COMPARISON

O Roslyn só reexecuta quando:

-   source type muda
-   destination type muda
-   attribute muda

----------

### STEP 3 — FLOW REUSE

```
If FlowSignature unchanged → reuse Flow
```

----------

### 🧩 7. FLOW SIGNATURE (CORE DO PERFORMANCE ENGINE)

### 📦 FlowSignature

```
FlowSignature
├── SourceTypeId
├── DestinationTypeId
├── PolicyHash
├── PropertyHash
```

----------

### HASH IDEA:

```
stable hash of:
- property names
- property types
- attributes
```

----------

### 🔁 8. FLOW CACHE FLOW

```
MappingCandidate
   ↓
Signature Generator
   ↓
FlowCache Lookup
   ↓
[HIT] → reuse Flow
   ↓
[MISS] → build Flow
   ↓
Store in Cache
```

----------

### 🧠 9. OPTIMIZAÇÃO DO SEMANTIC MODEL

### PROBLEMA:

SemanticModel é caro.

----------

### SOLUÇÃO v1:

### 🔵 Lazy Resolution

Só resolve semantic quando necessário:

```
Syntax → Candidate → only then SemanticModel
```

----------

### 🔵 Cache de Symbol Resolution

```
INamedTypeSymbol cache
```

----------

### ⚡ 10. FLOW CACHE IMPLEMENTATION

📄 FlowCache.cs

```csharp
using System.Collections.Concurrent;
using FlowMapper.Core.Domain;

namespace FlowMapper.SourceGenerator.Performance;

public class FlowCache
{
    private readonly ConcurrentDictionary<string, Flow> _cache = new();

    public bool TryGet(string key, out Flow flow)
        => _cache.TryGetValue(key, out flow);

    public void Set(string key, Flow flow)
        => _cache[key] = flow;
}
```

----------

### 🧠 11. FLOW KEY GENERATOR

```csharp
public static class FlowKeyGenerator
{
    public static string Create(INamedTypeSymbol source, INamedTypeSymbol dest)
    {
        return $"{source.ToDisplayString()}|{dest.ToDisplayString()}";
    }
}
```

----------

### 🧱 12. INTEGRAÇÃO NO FLOW BUILDER

Agora o FlowBuilder muda:

----------

### ANTES:

sempre construía Flow

----------

### AGORA:

```csharp
if (cache.TryGet(key, out var cachedFlow))
    return cachedFlow;

var flow = BuildFlow(...);

cache.Set(key, flow);
return flow;
```

----------

### 🚨 13. NOVO PROBLEMA RESOLVIDO

### ❌ antes:

-   reprocessamento em cada build
-   semantic overload
-   flatten recalculado sempre

----------

### ✔ agora:

-   reuse total de Flow
-   build incremental real
-   performance constante

----------

### 📦 14. FLOW CACHE + FLATTEN + CONSTRUCTOR

Agora todos os engines são cacheáveis:

-   Nested Engine ✔
-   Constructor Engine ✔
-   Flatten Engine ✔

----------

### ⚡ 15. RESULTADO DE PERFORMANCE

Você ganhou:

### 🚀 antes

-   O(n) por build completo

### 🚀 agora

-   ~O(1) para flows repetidos
-   incremental real
-   semantic usage reduzido drasticamente

----------

### 🧠 16. DECISÃO ARQUITETURAL IMPORTANTE

Agora o FlowMapper se torna:

> 🧠 state-aware during compilation (via cache lógico)

Mas ainda:

-   ❌ sem persistência externa
-   ❌ sem state entre soluções
-   ✔ state por compilation session

----------

### 🔥 17. O QUE VOCÊ ACABOU DE CONSTRUIR

Agora o FlowMapper tem:

✔ nested mapping engine  
✔ constructor mapping  
✔ flatten engine  
✔ diagnostics engine  
✔ incremental generator  
✔ performance cache engine

----------

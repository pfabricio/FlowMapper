## ⚠️ FLOWMAPPER v1 — DIAGNOSTICS ENGINE (Roslyn Analyzer + Generator)

### 🧠 1. VISÃO DO SISTEMA

O Diagnostics Engine é responsável por:

> Detectar problemas de mapeamento em tempo de compilação e reportar como erros/warnings do Roslyn.

Isso significa:

-   aparece no Error List do Visual Studio
-   quebra build se necessário
-   comportamento igual compilador C#

----------

### 🎯 2. OBJETIVO DO DIAGNOSTICS v1

Ele deve detectar:

#### 🚨 RM0001 — Propriedade de destino não mapeada

#### 🚨 RM0002 — Tipo incompatível

#### 🚨 RM0003 — Mapper inválido

#### 🚨 RM0004 — Mapeamento incompleto (source extra)

#### 🚨 RM0005 — Atributo Map mal formado

----------

### 🧱 3. ARQUITETURA DO DIAGNOSTICS ENGINE

Dividido em 2 camadas:

----------

### 🟢 CAMADA 1 — Generator Diagnostics (rápido)

Responsável por:

-   análise básica do Flow
-   checagem de convenção
-   emissão simples

📦 Vive dentro do Source Generator

----------

### 🔵 CAMADA 2 — Roslyn Analyzer (forte / IDE-level)

Responsável por:

-   erros no editor em tempo real
-   squiggles vermelhos no código
-   integração com Visual Studio / Rider

📦 Projeto separado: FlowMapper.Analyzers

----------

### ⚙️ 4. MODELO DE DIAGNÓSTICO

📄 FlowDiagnostic.cs

```csharp
using Microsoft.CodeAnalysis;

namespace FlowMapper.Analyzers;

public static class FlowDiagnostics
{
    public static readonly DiagnosticDescriptor MissingDestinationProperty =
        new(
            id: "FM0001",
            title: "Property not mapped",
            messageFormat: "Destination property '{0}' is not mapped",
            category: "FlowMapper",
            DiagnosticSeverity.Warning,
            isEnabledByDefault: true);

    public static readonly DiagnosticDescriptor TypeMismatch =
        new(
            id: "FM0002",
            title: "Type mismatch",
            messageFormat: "Cannot map '{0}' to '{1}'",
            category: "FlowMapper",
            DiagnosticSeverity.Error,
            isEnabledByDefault: true);

    public static readonly DiagnosticDescriptor InvalidMapper =
        new(
            id: "FM0003",
            title: "Invalid mapper",
            messageFormat: "Mapper '{0}' is invalid or incomplete",
            category: "FlowMapper",
            DiagnosticSeverity.Error,
            isEnabledByDefault: true);
}
```

----------

### 🧠 5. DIAGNOSTIC ENGINE (CORE LOGIC)

📄 FlowValidator.cs

```csharp
using FlowMapper.Core.Domain;

namespace FlowMapper.SourceGenerator.Pipeline.Validator;

public static class FlowValidator
{
    public static List<FlowDiagnosticResult> Validate(Flow flow)
    {
        var diagnostics = new List<FlowDiagnosticResult>();

        var mappedTargets = flow.Properties
            .Select(p => p.DestinationProperty)
            .ToHashSet();

        var allDestinationProps = GetAllDestinationProps(flow);

        // 🚨 RM0001 - Missing destination mapping
        foreach (var prop in allDestinationProps)
        {
            if (!mappedTargets.Contains(prop))
            {
                diagnostics.Add(FlowDiagnosticResult.Warning(
                    "FM0001",
                    $"Property '{prop}' is not mapped"));
            }
        }

        return diagnostics;
    }

    private static IEnumerable<string> GetAllDestinationProps(Flow flow)
    {
        // v1 simplificado (depois vira Roslyn Symbol)
        return new[] { "Id", "Name", "Email" };
    }
}
```

----------

### 📦 6. RESULTADO DE DIAGNÓSTICO

📄 FlowDiagnosticResult.cs

```csharp
namespace FlowMapper.SourceGenerator.Pipeline.Validator;

public class FlowDiagnosticResult
{
    public string Id { get; set; }
    public string Message { get; set; }
    public bool IsWarning { get; set; }

    public static FlowDiagnosticResult Warning(string id, string message)
        => new() { Id = id, Message = message, IsWarning = true };

    public static FlowDiagnosticResult Error(string id, string message)
        => new() { Id = id, Message = message, IsWarning = false };
}
```

----------

### ⚙️ 7. INTEGRAÇÃO COM SOURCE GENERATOR

Agora o generator precisa emitir diagnostics:

📄 FlowMapperGenerator.cs (trecho)

```csharp
private static void EmitSource(SourceProductionContext context, FlowModel model)
{
    foreach (var flow in model.Flows)
    {
        var diagnostics = FlowValidator.Validate(flow);

        foreach (var d in diagnostics)
        {
            var descriptor = d.IsWarning
                ? FlowDiagnostics.MissingDestinationProperty
                : FlowDiagnostics.TypeMismatch;

            context.ReportDiagnostic(
                Diagnostic.Create(
                    descriptor,
                    Location.None,
                    d.Message));
        }

        var code = FlowCodeGenerator.Generate(flow);

        context.AddSource($"{flow.SourceType}.g.cs", code);
    }
}
```

----------

### 🧠 8. CAMADA AVANÇADA (ANALYZER PROJETADO)

Agora o “nível compilador real”.

📦 FlowMapper.Analyzers

```csharp
using Microsoft.CodeAnalysis;
using Microsoft.CodeAnalysis.Diagnostics;

[DiagnosticAnalyzer(LanguageNames.CSharp)]
public class FlowMapperAnalyzer : DiagnosticAnalyzer
{
    public override ImmutableArray<DiagnosticDescriptor> SupportedDiagnostics
        => ImmutableArray.Create(
            FlowDiagnostics.MissingDestinationProperty,
            FlowDiagnostics.TypeMismatch);

    public override void Initialize(AnalysisContext context)
    {
        context.ConfigureGeneratedCodeAnalysis(
            GeneratedCodeAnalysisFlags.None);

        context.EnableConcurrentExecution();

        context.RegisterSymbolAction(AnalyzeSymbol, SymbolKind.NamedType);
    }

    private void AnalyzeSymbol(SymbolAnalysisContext context)
    {
        var symbol = (INamedTypeSymbol)context.Symbol;

        var hasMapAttribute = symbol
            .GetAttributes()
            .Any(a => a.AttributeClass?.Name == "MapAttribute");

        if (!hasMapAttribute)
            return;

        // aqui no v1 apenas valida estrutura
        if (!symbol.IsPartial())
        {
            context.ReportDiagnostic(Diagnostic.Create(
                FlowDiagnostics.InvalidMapper,
                symbol.Locations.FirstOrDefault(),
                symbol.Name));
        }
    }
}
```

----------

### ⚡ 9. DIFERENCIAL IMPORTANTE

Agora o FlowMapper tem:

#### ✔ Generator diagnostics (build time)

#### ✔ Analyzer diagnostics (editor time)

Isso significa:

> O erro aparece ANTES de compilar

----------

### 🧭 10. EXPERIÊNCIA DO DESENVOLVEDOR

### Antes:

-   só descobre erro em runtime ou build

### Agora:

-   erro aparece em tempo real no editor
-   squiggles vermelhos
-   mensagens tipo compilador C#

----------

### 🚨 11. REGRAS DO DIAGNOSTICS v1

-   não bloquear build por padrão (warnings first)
-   erros só para inconsistência estrutural grave
-   mensagens devem ser claras e curtas
-   IDs sempre FM0001+

----------

### 🧱 12. RESULTADO FINAL DO ENGINE

Agora FlowMapper tem:

✔ Source Generator  
✔ Flow Engine  
✔ Convention mapping  
✔ Code generation  
✔ Diagnostics engine (generator + analyzer)  
✔ IDE integration

----------